# ATTACK LEDGER — curated from timeline.jsonl

The complete append-only, hash-chained record ships as `timeline.jsonl`.
(16 records shown)
Below: every attack/finding record this timeline pre-registered or measured.

## seq 10 — prediction [proto/tsh-padding] PREDICTION: TSH's 10*1 padding is NON-INJECTIVE. padded = data+0x80, zero-fill to 32-multiple, then padded[-1] is overwritten with 0x01 - so the 0x80 marker is destroyed whenever it lands on a block's last byte (len(data) mod 32 == 31). Consequence: for EVERY X with len(X) mod 32 == 30, pad(X) == pad(X||0x80), hence TSH(X) == TSH(X||0x80) - free contrived collisions, to be measured on 30-byte and …

## seq 13 — finding [proto/tsh-padding] PREDICTION seq-10 CONFIRMED - TSH-512 COLLISION RESISTANCE IS BROKEN BY CONSTRUCTION. Measured: for len(X) mod 32 == 30, TSH(X) == TSH(X||0x80), verified at lengths 30/62/94/126 (e.g. bytes(range(30)) and bytes(range(30))+0x80 share digest f7024d42e4345cc4...). Cause: 10*1 padding appends 0x80 then overwrites the FINAL byte with 0x01, destroying the marker whenever it lands on a block's last byte …

## seq 17 — finding [proto/pdh-tamper-blindspots] PHASE 0 TAMPER GATE, measured (45 tampers: every PHASE/ROUND constant bit-0 flipped, MIX multipliers, all 8 PDH initial words, gate rotation, feistel shift, column-mix rotation, PDH cross-mix offset, PDH final rounds, TSH pad byte). JOINT gate: 45/45 PASS - the frozen fixture detects every tamper via at least one digest (TSH catches what PDH misses). STRICT gate: 27/45 - 18 measured blind spots, a…

## seq 34 — finding [t1/class-invariance] T1 STRUCTURAL KILL (measured, then diagnosed): one input-trit flip changes only ~7% of output trits and 54 output positions are unreachable. Per-round differential trace (18 rounds, one-trit initial difference): the difference NEVER leaves 3 of 27 lanes (2-16 differing trits of 162 at every round). CAUSE: T1's theta mixes lane triples {c, c+9, c+18} (same residue mod 9), and pi l->(7l+3) mod 27 is…

## seq 37 — finding [family/tick-replay] ATTACK COURT (Phase 4 opening): TICK-CONGRUENCE REPLAY HOLE confirmed for ALL FOUR family constructions - measured hash(msg, tick=t) === hash(msg, tick=t+729) === hash(msg, tick=t+2*729) for every t and msg tested (T1-T4, e.g. msg='ledger-entry-42', tick 5/734/1463 identical full 486-trit digests). Cause: every construction folds the tick through balanced tryte wrap (padding tryte AND all seeded c…

## seq 40 — prediction [attack/t2-reduced-rounds] PREDICTION (pre-registered before measuring): reduced-round T2 (_T2_ROUNDS in {2,4,6,8}, all else unchanged) shows a diffusion staircase. rounds=2: mean output-trit change rate well BELOW the [0.6,0.73] band (I predict <0.5) with many positions near 0 (G-steps at 2 rounds touch only sparse word pairs). rounds=4: mean inside or near band but >=10 positions outside. rounds>=6: statistically indistin…

## seq 41 — prediction [attack/t4-passes-and-expander-masking] PREDICTION: T4 with _T4_PASSES in {1,2} will expose an INSTRUMENT BLINDNESS, not just a weakness: the output expander _expand_to_81 chains every output tryte through c=c*7+w[j%16]+seed, so even a 16-tryte state with only a few changed words drives nearly every output tryte to change - I predict OUTPUT-level avalanche stays inside [0.6,0.73] even at passes=1 (expander MASKS state holes), while STAT…

## seq 42 — prediction [attack/exact-differential-repeats] PREDICTION: fixed single-trit input differences at weakest parameters (T2 rounds=2, T4 passes=1) will produce at least one EXACT repeated 486-trit output difference over ~1,500 random base inputs - a distinguishable differential characteristic (for a random function the expected repeat count is ~C(1500,2)/3^486 = 0). At full parameters (T2 rounds=10, T4 passes=3) I predict zero repeats. If zero re…

## seq 43 — finding [t2/dead-pipe] ATTACK COURT REDUCED-ROUND (T2, _T2_ROUNDS 2/4/6/8/10, n=7386 output + n=2226 state): T2's BLAKE-shaped DOUBLE PIPE IS DECORATION. Measured state-level: mean change rate 0.3338-0.3341 IDENTICALLY at every round count, 96/192 pipe-state trits below 0.6, min 0.0000 - one entire pipe (h[16..31], 96 trits) NEVER receives message differences at ANY round count. Code-level cause confirmed by the measure…

## seq 46 — finding [t3/3adic-kernel] ATTACK COURT (T3-W1, pre-registered seq 28): the Z/729 ring structure gives T3's round function a MASSIVE 3-ADIC KERNEL, measured exhaustively. For the real key-chain coefficients at rounds 0/5/15: f_i(x) == f_i(x+243) for exactly 243/729 = 1/3 of all x at positions where the quadratic coefficient a2 = kr+i is not divisible by 3 (measured 243/729 at i=0 and i=26, 0/729 at i=7 where a2 = kr+7 = 0 m…

## seq 57 — prediction [attack-v2/t1-rounds] PREDICTION (before measuring): T1-v2 reduced-round differential sweep (_T1_ROUNDS in {2,4,6}, n=10,000 flips, same protocol as the gate): with cross-class theta, rounds=2 mean well below the band (<0.5) with many positional holes; rounds=4 mean near 0.55-0.62 with >=20 positions outside [0.6,0.73]; the TRANSITION to all-positions-in-band happens at rounds=6 (my claimed transition point). Falsified…

## seq 58 — prediction [attack-v2/t4-masking] PREDICTION: T4-v2 (compression unchanged from v1; only padding shifted) reproduces the v1 masking signature at reduced passes: passes=1 output mean <0.66 with >=20/486 outside band AND state 96/96 below 0.6; passes=2 output all-in-band (0/486) while state has >=40/96 below 0.6 - the expander-masked compression the state gate now witnesses. Falsified if the padding shift materially changes any of t…

## seq 59 — prediction [attack-v2/exact-differentials] PREDICTION: exact differential characteristics on v2 at weakest parameters (T1-v2 rounds=2, T4-v2 passes=1; fixed trit0-up flip, n=1500): ZERO exact 486-trit output-difference repeats, same null result as v1 (seq 45) - no high-probability characteristic at reduced rounds within budget. If a repeat appears, v2 is WORSE than v1 and that is a finding; zero everywhere = second survived-budget data poi…

## seq 76 — prediction [attack/slide-probes] PREDICTION (slide/structural-relation battery on merge candidates T1/T2/T4, before measuring): all probes NULL at stated budgets - (a) block-order commutation: t2_state(B1||B2) == t2_state(B2||B1) and same for t4_state over 500 random block pairs each: 0 equal; (b) absorbing states: t4_compress(s, zero-block) == s for 0 of 1000 random states, and T2 absorbing an all-zero message block leaves state…

## seq 77 — prediction [attack/slide-distance] PREDICTION (same battery, rate level): block-order commutation at state level changes a RANDOM-LIKE fraction of state trits - T2 mean change rate in [0.6, 0.73] over the 192 pipe trits and T4 in [0.6, 0.73] over 96 chaining trits when comparing state(B1||B2) vs state(B2||B1); and T4's zero-block compression changes ~2/3 of state trits from a random state (no absorbing tendency: mean change rate > …

## seq 81 — prediction [attack/multicollision-prefix] PREDICTION (docket item d instrument, before measuring): single-level birthday probe on T4's truncated chaining state - absorb N=28,000 distinct single blocks from the fixed tick-0 initial state, count collisions on the 3-tryte (24-trit... 729^3 = 3.87e8 space) state prefix. Poisson expectation at n=28k is ~1.01 collisions; I predict T4 lands in [0, 5] (P(>=6) ~ 6e-4) and the blake2b-modeled contr…

